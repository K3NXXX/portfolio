class Pages {
	private BASE_URL = '/portfolio'
	INTRO = `${this.BASE_URL}/intro`
	BEGINNING = `${this.BASE_URL}/beginning`
	PROJECT = `${this.BASE_URL}/project`
	PROJECTS = `${this.BASE_URL}/projects`
	ABOUT_ME = `${this.BASE_URL}/about-me`
	ACHIEVEMENTS = `${this.BASE_URL}/achievements`
}

export const PAGES = new Pages()
