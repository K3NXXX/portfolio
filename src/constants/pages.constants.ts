class Pages {
	private BASE_URL = '/';
	INTRO = `${this.BASE_URL}intro`;
	PORTFOLIO = `${this.BASE_URL}portfolio`;
	PROJECT = `${this.PORTFOLIO}/project`; 
  }
  
  export const PAGES = new Pages();
  